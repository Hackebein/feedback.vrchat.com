# Feedback search edge: per-remote-IP concurrency and sustained request rate caps
# toward the gateway upstream (deploy/nginx/sites/https-public.conf.template).
limit_conn_zone $binary_remote_addr zone=gateway_parallel:10m;
limit_req_zone $binary_remote_addr zone=gateway_rps:10m rate=15r/s;
